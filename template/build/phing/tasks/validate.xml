<project name="validate" default="validate:all">

  <!-- Run all validation targets. -->
  <target name="validate:all" description="Runs all code validation targets."
          depends="validate:lint, validate:phpcs, validate:phpmd"/>

  <!-- Run linter against all php files in repository. -->
  <target name="validate:lint" description="Runs a PHP Lint against all code.">
    <phplint>
      <fileset refid="files.php.all"/>
    </phplint>
  </target>

  <!-- Run code sniffer against all custom code. -->
  <target name="validate:phpcs" description="Runs PHP Code Sniffer against only custom code.">
    <phpcodesniffer
        standard="${repo.root}/vendor/drupal/coder/coder_sniffer/Drupal/"
        showSniffs="true"
        showWarnings="true"
        haltonerror="true"
        haltonwarning="true">
      <fileset refid="files.php.custom"/>
      <formatter type="full" usefile="false"/>
    </phpcodesniffer>

    <phpcodesniffer
        standard="PSR2"
        showSniffs="true"
        showWarnings="true"
        haltonerror="true"
        haltonwarning="true">
      <fileset refid="files.tests"/>
      <formatter type="full" usefile="false"/>
    </phpcodesniffer>
  </target>

  <!-- Run code complexity and cleanliness checks on custom code. -->
  <target name="validate:phpmd" description="Runs PHP Mess Detector against only custom code.">
    <phpmd rulesets="codesize,unusedcode,cleancode">
      <fileset refid="files.php.custom"/>
      <formatter type="text" outfile="false" usefile="false"/>
    </phpmd>
  </target>
</project>
