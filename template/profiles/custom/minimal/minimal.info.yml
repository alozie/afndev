name: ${project.human_name}
type: profile
description: 'Build a custom site without pre-configured functionality.'
core: 8.x
dependencies:
  - node
  - block
  - dblog
  - page_cache
  - dynamic_page_cache
themes:
  - stark
